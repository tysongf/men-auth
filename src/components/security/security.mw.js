const Users = require("../users/users.model");

export const rateLimit = (req, res) => {};
export const isAuthentic = (req, res) => {
   //they have a token
   //the token is valid
   //get user id from jwt
   //attach the user to the req
   //req.user = User
};
